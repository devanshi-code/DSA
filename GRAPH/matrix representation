//matrix
int main()
{
    int graph[5][5];
    memset(graph,0,sizeof graph);
     m[0][3]=1;
     m[1][2]=1;
}

//another way for weighted graphs
int main()
{
    vector<vector<int>> graph;
    graph.push_back({source,destination,weight});
    //eg : graph.push_back({1,3,50});
}    
